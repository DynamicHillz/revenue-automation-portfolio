name: Keep App Alive
on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping all pages
        run: |
          curl -f https://portfoliolandingpy.streamlit.app/
          sleep 10
          curl -f https://leadintelligencedemopy.streamlit.app/
          sleep 10
          curl -f https://customersuccessragpy.streamlit.app/
          sleep 10
          curl -f https://revenuedashboardpy.streamlit.app/
